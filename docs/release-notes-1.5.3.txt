auth-pro 1.5.3

管理端敏感写接口按角色菜单鉴权。没有对应菜单权限的管理员，不能再只凭管理员令牌调用授权、卡密、应用等写接口。超级管理员仍放行。
在线更新要求包签名为 sha256: 加与包 SHA256 相同的 64 位十六进制，并核对 GitHub Release 附件摘要。前端目录不是符号链接时，先写入暂存目录再原子改名切换，避免更新中途半新半旧。
源站破坏性结构变更改为命名迁移。删列前先回填数据。迁移失败不标记已应用，启动直接退出。
