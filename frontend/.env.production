# 【生产】环境变量

# 版本号（与根目录 VERSION / 后端 AppVersion 对齐）
VITE_VERSION = 1.5.0

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# API 地址前缀（与后端同源部署）
VITE_API_URL = /

# 侧栏以后端菜单为准（「系统 → 菜单管理」）
VITE_ACCESS_MODE = backend

# Delete console
VITE_DROP_CONSOLE = true